name: Build

on:
  push:
    branches: [main]
    tags:
      - 'v*'
    paths:
      - 'cli.py'
      - 'cli.spec'
      - 'main.spec'
      - 'cli/**'
      - 'src/**'
      - 'gui/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-cli:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            artifact: xhs-cli.exe
            spec_path: "cli.spec"
          - platform: macos-latest
            artifact: xhs-cli
            spec_path: "cli.spec"

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build CLI (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          pyinstaller cli.spec

      - name: Build CLI (macOS)
        if: matrix.platform == 'macos-latest'
        run: |
          pyinstaller cli.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-cli
          path: dist/${{ matrix.artifact }}
          retention-days: 1

  build-desktop:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            artifact: xhs_idea_tool.exe
          - platform: macos-latest
            artifact: xhs_idea_tool.app
          - platform: ubuntu-latest
            artifact: xhs_idea_tool

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Install Playwright browsers
        run: |
          playwright install chromium
          playwright install firefox
          playwright install webkit

      - name: Build with PyInstaller (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          pyinstaller main.spec

      - name: Build with PyInstaller (macOS)
        if: matrix.platform == 'macos-latest'
        run: |
          pyinstaller main.spec
          cd dist
          mkdir -p xhs_idea_tool.app/Contents/MacOS
          mv xhs_idea_tool xhs_idea_tool.app/Contents/MacOS/

      - name: Build with PyInstaller (Linux)
        if: matrix.platform == 'ubuntu-latest'
        run: |
          pyinstaller main.spec

      - name: Upload artifact (Windows)
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-desktop
          path: dist/xhs_idea_tool.exe
          retention-days: 1

      - name: Upload artifact (macOS)
        if: matrix.platform == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-desktop
          path: dist/xhs_idea_tool.app
          retention-days: 1

      - name: Upload artifact (Linux)
        if: matrix.platform == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-desktop
          path: dist/xhs_idea_tool
          retention-days: 1

  release:
    needs: [build-cli, build-desktop]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: |
          find artifacts -type f -name "*"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
